# Classical SER baseline (eGeMAPS + SVM) - CPU only, no GPU needed.
# Versions below were validated on Python 3.14. On another machine, plain
# `pip install -r requirements.txt` should resolve compatible wheels.
numpy>=2.0
pandas>=2.0
scipy>=1.13
scikit-learn>=1.5
matplotlib>=3.8
seaborn>=0.13
opensmile>=2.5
soundfile>=0.12
librosa>=0.10
